# Sao chép file này thành "keystore.properties" (cùng thư mục, ngang hàng
# settings.gradle) rồi điền thông tin thật của bạn. KHÔNG commit file
# "keystore.properties" (đã có sẵn trong file thật) lên git/GitHub vì chứa
# mật khẩu — chỉ commit file .example này thôi.

storeFile=../com11h-release.jks
storePassword=MAT_KHAU_KEYSTORE
keyAlias=com11h
keyPassword=MAT_KHAU_KEY_ALIAS
